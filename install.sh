#!/bin/bash

# This installation is only for the archlinux-logout
# this isn't an official script and this works for me
# I cannot guarantee that it's going to work for you.

cp -r usr/share/archlinux-logout usr/share/archlinux-logout-themes /usr/share/
cp usr/bin/archlinux-logout /usr/bin/
